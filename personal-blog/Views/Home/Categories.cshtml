@using personal_blog.Models
@model List<Category>

<div class="sidebar-box">
    <h3 class="heading">Categories</h3>
    <ul class="categories">
        @foreach (var x in Model)
        {
            <li><a href="/Category/Posts/@x.ID">@x.Name <span>(12)</span></a></li>
        }
    </ul>
</div>